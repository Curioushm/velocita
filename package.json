{
  "name": "ve",
  "version": "1.0.0",
  "description": "A modern e-commerce platform for electronic components and development boards.",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "both": "concurrently \"npm run client\" \"npm run server\"",
    "client:install": "cd client && npm install",
    "server:install": "cd server && npm install",
    "install": "concurrently \"npm run client:install\" \"npm run server:install\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "concurrently": "^9.1.2",
    "framer-motion": "^12.9.4",
    "react-redux": "^9.2.0",
    "react-toastify": "^11.0.5"
  }
}
